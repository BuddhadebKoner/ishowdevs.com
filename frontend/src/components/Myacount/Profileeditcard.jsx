import React from 'react';

export default function Myacount(
   { fullName, mobile, portfolio, workAs, keyWords, mediaLinks }
) {

   console.log(fullName, mobile, portfolio, workAs, keyWords, mediaLinks);


   return (

      <>
         hi
      </>

   )

}